<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/overview_graph.xml"
    app:startDestination="@id/overviewFragment">

    <fragment
        android:id="@+id/overviewFragment"
        android:name="com.example.infits.OverviewFragment"
        android:label="fragment_overview"
        tools:layout="@layout/fragment_overview" >
        <action
            android:id="@+id/action_overviewFragment_to_chatArea"
            app:destination="@id/chatArea" />
    </fragment>
    <activity
        android:id="@+id/chatArea"
        android:name="com.example.infits.ChatArea"
        android:label="activity_chat_area"
        tools:layout="@layout/activity_chat_area" />
</navigation>